import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const HardCurrency = () => {
  return (
    <View>
      <Text>HardCurrency</Text>
    </View>
  )
}

export default HardCurrency

const styles = StyleSheet.create({})